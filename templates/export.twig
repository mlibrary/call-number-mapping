Topic	alphaStart	numStart	cutStart	alphaEnd	numEnd	cutEnd	notes
{% for topic in topics %}{{ topic.name }}	{{ topic.alphaStart }}	{{ topic.numStart }}	{{ topic.cutStart }}	{{ topic.alphaEnd }}	{{ topic.numEnd }}	{{ topic.cutEnd }}	{{ topic.notes }}
{% endfor %}
