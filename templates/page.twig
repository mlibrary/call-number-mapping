<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Docs: https://design-system.lib.umich.edu/reusable-designs/website-header/ -->
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    <!-- Place this in the <head> -->
    <link href="https://cdn.jsdelivr.net/npm/@umich-lib/web@latest/umich-lib.css" rel="stylesheet"/>
    <link href="hlb.css" rel="stylesheet"/>
    <script type="module" src="https://cdn.jsdelivr.net/npm/@umich-lib/web@latest/dist/umich-lib/umich-lib.esm.js"></script>
  </head>
  <body>
    <!-- Place this in the <body> -->
    <div>
      <m-website-header name="Call Number Mapping" variant="dark">
        {% if authenticated %}
          <a href="https://shibboleth.umich.edu/cgi-bin/logout?https://staff.lib.umich.edu/" style="color: white;">Log out</a>
        {% else %}
          <a href="/admin" style="color: white;">Log in</a>
        {% endif %}
      </m-website-header>
    </div>
    <main class="viewport-container">
      {% for section in content %}
      <div class="section">
        {{ section }}
      </div>
      {% else %}
        <div class="section">
          No content to display.
        </div>
      {% endfor %}
    </main>
  </body>
</html>
